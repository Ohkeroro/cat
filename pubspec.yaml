name: cat
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.4  # สำหรับเลือกรูปภาพจากไฟล์
  path_provider: ^2.1.2 # ใช้เก็บไฟล์ลง storage
  google_fonts: ^6.2.1  # ใช้ฟอนต์ภาษาไทย (Noto Sans Thai)
  permission_handler: ^11.0.1  # ใช้ขอสิทธิ์เข้าถึงไฟล์ (สำหรับ Android)



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
